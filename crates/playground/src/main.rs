fn main() {
    // bounded_channel::run(2);
    // atomic_counter::run();
    // box_dyn_traits::run(box_dyn_traits::Config {
    //     weather_service: "openweather".to_string(),
    // });

    // trait_bounds::run();
    // trait_enums::run();
}
